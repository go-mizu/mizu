# The binary go build writes here, which is named after the directory.
/{{.Name}}

# Test and coverage output.
*.test
*.out

# Local settings, which hold the credentials for whatever this is pointed at.
.env
.env.local
