// Auto-generated from docs.md — do not edit
export const docsContentHtml = "<h1 id=\"overview\">API Reference</h1>\n<p>Complete reference for the storage.now API. Organize files into buckets, upload and download objects, and generate time-limited signed URLs for secure sharing — all through a consistent REST interface inspired by Supabase Storage.</p>\n<p>Base URL: <code>https://storage.now</code></p>\n<p>All requests and responses use JSON, except file uploads and downloads which transfer raw bytes with the appropriate <code>Content-Type</code> header. Errors follow a consistent shape: <code>{&quot;error&quot;: {&quot;code&quot;: &quot;...&quot;, &quot;message&quot;: &quot;...&quot;}}</code>.</p>\n<h2 id=\"quickstart\">Quick Start</h2>\n<p>Get up and running in five steps. Register an identity, authenticate, create a bucket, upload a file, and share it with a signed URL.</p>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code># 1. Register an agent identity\ncurl -X POST storage.now/auth/register \\\n  -H &quot;Content-Type: application/json&quot; \\\n  -d '{&quot;actor&quot;:&quot;a/my-app&quot;,&quot;public_key&quot;:&quot;&lt;base64url-ed25519&gt;&quot;}'\n\n# 2. Request a challenge and sign it\ncurl -X POST storage.now/auth/challenge \\\n  -d '{&quot;actor&quot;:&quot;a/my-app&quot;}'\n# → sign the nonce with your Ed25519 private key, then:\ncurl -X POST storage.now/auth/verify \\\n  -d '{&quot;challenge_id&quot;:&quot;ch_...&quot;,&quot;actor&quot;:&quot;a/my-app&quot;,&quot;signature&quot;:&quot;&lt;base64url&gt;&quot;}'\n# → {&quot;access_token&quot;:&quot;...&quot;}\n\n# 3. Create a bucket\ncurl -X POST storage.now/bucket \\\n  -H &quot;Authorization: Bearer $TOKEN&quot; \\\n  -d '{&quot;name&quot;:&quot;my-files&quot;,&quot;public&quot;:false}'\n\n# 4. Upload a file\ncurl -X PUT storage.now/object/my-files/hello.txt \\\n  -H &quot;Authorization: Bearer $TOKEN&quot; \\\n  -H &quot;Content-Type: text/plain&quot; \\\n  -d &quot;Hello, world!&quot;\n\n# 5. Generate a signed URL to share it\ncurl -X POST storage.now/object/sign/my-files \\\n  -H &quot;Authorization: Bearer $TOKEN&quot; \\\n  -d '{&quot;path&quot;:&quot;hello.txt&quot;,&quot;expires_in&quot;:3600}'\n# → {&quot;signed_url&quot;:&quot;/sign/tok_abc123&quot;}</code></pre>\n<h2 id=\"authentication\">Authentication</h2>\n<p>All routes except <code>/auth/*</code>, <code>/sign/*</code>, <code>/upload/sign/*</code>, and <code>/object/public/*</code> require a Bearer token in the <code>Authorization</code> header. Tokens are obtained through one of three authentication methods, depending on your use case.</p>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>Authorization: Bearer &lt;token&gt;</code></pre>\n<table class=\"ref-table\">\n<thead><tr><th>Method</th><th>Best for</th><th>How it works</th></tr></thead>\n<tbody>\n<tr><td>Ed25519</td><td>Machines, scripts, CI/CD</td><td>Register an Ed25519 public key, then sign server-issued challenges to prove identity. Tokens expire after 2 hours.</td></tr>\n<tr><td>Magic link</td><td>Human users</td><td>Provide an email address to receive a one-time login link. Clicking the link sets a session cookie.</td></tr>\n<tr><td>API key</td><td>Long-lived integrations</td><td>Generate a scoped API key via <code>POST /keys</code>. Use the returned <code>sk_...</code> token as a Bearer token. Keys can be restricted to specific operations and path prefixes.</td></tr>\n</tbody></table>\n<h2 id=\"rate-limits\">Rate Limits</h2>\n<p>The API enforces per-IP sliding window rate limits. When a limit is exceeded, the server responds with <code>429 Too Many Requests</code> and a <code>Retry-After</code> header indicating how many seconds to wait before retrying.</p>\n<table class=\"ref-table\">\n<thead><tr><th>Endpoint</th><th>Limit</th><th>Window</th></tr></thead>\n<tbody>\n<tr><td>POST /auth/register</td><td>10 requests</td><td>1 minute</td></tr>\n<tr><td>POST /auth/challenge</td><td>20 requests</td><td>1 minute</td></tr>\n<tr><td>POST /auth/magic-link</td><td>5 requests</td><td>1 minute</td></tr>\n<tr><td>PUT /object/*</td><td>100 requests</td><td>1 minute</td></tr>\n<tr><td>GET /sign/*</td><td>200 requests</td><td>1 minute</td></tr>\n</tbody></table>\n<h1 id=\"buckets\">Buckets</h1>\n<p>Buckets are top-level containers that organize your objects into logical groups — similar to S3 buckets or filesystem mount points. Each bucket belongs to a single owner and has its own visibility setting, file size limit, and MIME type allowlist.</p>\n<p>Public buckets expose their objects for unauthenticated read access at <code>/object/public/:bucket/*path</code>, making them ideal for serving avatars, static assets, or any content that should be freely accessible. Private buckets require a valid Bearer token or signed URL for every operation.</p>\n<h2 id=\"bucket-create\"><span class=\"method method--post\">POST</span> /bucket</h2>\n<p>Creates a new bucket. Bucket names must be globally unique within your account, between 2 and 63 characters long, and contain only lowercase letters, numbers, dots, hyphens, and underscores. The name must start with a letter or number.</p>\n<p>You can optionally set a <code>file_size_limit</code> to reject uploads that exceed a certain size, and an <code>allowed_mime_types</code> array to restrict which file types can be stored. Both constraints are enforced at upload time and apply to all objects in the bucket.</p>\n<p>Returns the newly created bucket object with a <code>201 Created</code> status.</p>\n<p><strong>Request body</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>name</td><td>string</td><td>Yes</td><td>Bucket name. Must be 2–63 characters, lowercase alphanumeric with <code>.</code>, <code>-</code>, <code>_</code>. Must start with a letter or number. Example: <code>&quot;avatars&quot;</code>, <code>&quot;project-assets&quot;</code>.</td></tr>\n<tr><td>public</td><td>boolean</td><td>No</td><td>Whether objects in this bucket are readable without authentication. When set to <code>true</code>, objects are accessible at <code>/object/public/:bucket/*path</code>. Defaults to <code>false</code>.</td></tr>\n<tr><td>file_size_limit</td><td>integer</td><td>No</td><td>Maximum allowed file size in bytes for objects uploaded to this bucket. Set to <code>null</code> or omit to allow files of any size. For example, <code>5242880</code> limits uploads to 5 MB.</td></tr>\n<tr><td>allowed_mime_types</td><td>string[]</td><td>No</td><td>Array of MIME type strings that are permitted for upload. For example, <code>[&quot;image/png&quot;, &quot;image/jpeg&quot;, &quot;image/webp&quot;]</code> restricts the bucket to image files only. Set to <code>null</code> or omit to allow any content type.</td></tr>\n</tbody></table>\n<p><strong>Response</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>id</td><td>string</td><td>Unique bucket identifier, prefixed with <code>bk_</code>. Example: <code>&quot;bk_a1b2c3&quot;</code>.</td></tr>\n<tr><td>name</td><td>string</td><td>The bucket name as provided in the request.</td></tr>\n<tr><td>owner</td><td>string</td><td>Actor identifier of the bucket owner.</td></tr>\n<tr><td>public</td><td>boolean</td><td>Whether the bucket allows unauthenticated reads.</td></tr>\n<tr><td>file_size_limit</td><td>integer|null</td><td>Maximum file size in bytes, or <code>null</code> if unrestricted.</td></tr>\n<tr><td>allowed_mime_types</td><td>string[]|null</td><td>Allowed MIME types, or <code>null</code> if unrestricted.</td></tr>\n<tr><td>created_at</td><td>integer</td><td>Creation timestamp as Unix epoch in milliseconds.</td></tr>\n</tbody></table>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl -X POST storage.now/bucket \\\n  -H &quot;Authorization: Bearer $TOKEN&quot; \\\n  -H &quot;Content-Type: application/json&quot; \\\n  -d '{\n    &quot;name&quot;: &quot;avatars&quot;,\n    &quot;public&quot;: true,\n    &quot;file_size_limit&quot;: 5242880,\n    &quot;allowed_mime_types&quot;: [&quot;image/png&quot;, &quot;image/jpeg&quot;]\n  }'</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/bucket&quot;, {\n  method: &quot;POST&quot;,\n  headers: {\n    &quot;Authorization&quot;: `Bearer ${TOKEN}`,\n    &quot;Content-Type&quot;: &quot;application/json&quot;,\n  },\n  body: JSON.stringify({\n    name: &quot;avatars&quot;,\n    public: true,\n    file_size_limit: 5242880,\n    allowed_mime_types: [&quot;image/png&quot;, &quot;image/jpeg&quot;],\n  }),\n});\nconst bucket = await res.json();</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{\n  &quot;id&quot;: &quot;bk_a1b2c3&quot;,\n  &quot;name&quot;: &quot;avatars&quot;,\n  &quot;owner&quot;: &quot;a/my-app&quot;,\n  &quot;public&quot;: true,\n  &quot;file_size_limit&quot;: 5242880,\n  &quot;allowed_mime_types&quot;: [&quot;image/png&quot;, &quot;image/jpeg&quot;],\n  &quot;created_at&quot;: 1710892800000\n}</code></pre>\n<h2 id=\"bucket-list\"><span class=\"method method--get\">GET</span> /bucket</h2>\n<p>Returns a list of all buckets owned by the authenticated actor. Each bucket in the response includes its name, visibility, and creation timestamp. Use this to discover available buckets before listing their contents.</p>\n<p><strong>Response</strong> — Array of bucket objects.</p>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl storage.now/bucket \\\n  -H &quot;Authorization: Bearer $TOKEN&quot;</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/bucket&quot;, {\n  headers: { &quot;Authorization&quot;: `Bearer ${TOKEN}` },\n});\nconst buckets = await res.json();</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>[\n  {&quot;id&quot;: &quot;bk_a1b2c3&quot;, &quot;name&quot;: &quot;avatars&quot;, &quot;public&quot;: true, &quot;created_at&quot;: 1710892800000},\n  {&quot;id&quot;: &quot;bk_d4e5f6&quot;, &quot;name&quot;: &quot;documents&quot;, &quot;public&quot;: false, &quot;created_at&quot;: 1710892900000}\n]</code></pre>\n<h2 id=\"bucket-get\"><span class=\"method method--get\">GET</span> /bucket/:id</h2>\n<p>Retrieves detailed information about a single bucket, including storage statistics such as the total number of objects and their combined size. This is useful for monitoring bucket usage and displaying storage dashboards.</p>\n<p><strong>Path parameters</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>id</td><td>string</td><td>The bucket ID. Example: <code>&quot;bk_a1b2c3&quot;</code>.</td></tr>\n</tbody></table>\n<p><strong>Response</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>id</td><td>string</td><td>Bucket ID.</td></tr>\n<tr><td>name</td><td>string</td><td>Bucket name.</td></tr>\n<tr><td>owner</td><td>string</td><td>Actor who created the bucket.</td></tr>\n<tr><td>public</td><td>boolean</td><td>Whether the bucket allows unauthenticated reads.</td></tr>\n<tr><td>file_size_limit</td><td>integer|null</td><td>Maximum file size in bytes, or <code>null</code> if unrestricted.</td></tr>\n<tr><td>allowed_mime_types</td><td>string[]|null</td><td>Allowed MIME types, or <code>null</code> if unrestricted.</td></tr>\n<tr><td>object_count</td><td>integer</td><td>Total number of objects currently stored in the bucket.</td></tr>\n<tr><td>total_size</td><td>integer</td><td>Combined size of all objects in bytes.</td></tr>\n<tr><td>created_at</td><td>integer</td><td>Creation timestamp in milliseconds.</td></tr>\n<tr><td>updated_at</td><td>integer</td><td>Last modification timestamp in milliseconds.</td></tr>\n</tbody></table>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl storage.now/bucket/bk_a1b2c3 \\\n  -H &quot;Authorization: Bearer $TOKEN&quot;</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/bucket/bk_a1b2c3&quot;, {\n  headers: { &quot;Authorization&quot;: `Bearer ${TOKEN}` },\n});\nconst bucket = await res.json();</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{\n  &quot;id&quot;: &quot;bk_a1b2c3&quot;,\n  &quot;name&quot;: &quot;avatars&quot;,\n  &quot;owner&quot;: &quot;a/my-app&quot;,\n  &quot;public&quot;: true,\n  &quot;file_size_limit&quot;: 5242880,\n  &quot;allowed_mime_types&quot;: [&quot;image/png&quot;, &quot;image/jpeg&quot;],\n  &quot;object_count&quot;: 142,\n  &quot;total_size&quot;: 89456000,\n  &quot;created_at&quot;: 1710892800000,\n  &quot;updated_at&quot;: 1710903600000\n}</code></pre>\n<h2 id=\"bucket-update\"><span class=\"method method--patch\">PATCH</span> /bucket/:id</h2>\n<p>Updates the configuration of an existing bucket. Only the fields you include in the request body are modified — all other settings remain unchanged. This is useful for toggling visibility, adjusting upload constraints, or relaxing MIME type restrictions without recreating the bucket.</p>\n<p>Changing a bucket from private to public immediately exposes all existing objects at <code>/object/public/:bucket/*path</code>. Changing from public to private immediately revokes unauthenticated access.</p>\n<p><strong>Path parameters</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>id</td><td>string</td><td>The bucket ID to update.</td></tr>\n</tbody></table>\n<p><strong>Request body</strong> — All fields are optional. Only provided fields are updated.</p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>public</td><td>boolean</td><td>Change the bucket&#39;s visibility. Setting to <code>true</code> makes objects publicly readable; <code>false</code> requires authentication.</td></tr>\n<tr><td>file_size_limit</td><td>integer|null</td><td>Change the maximum allowed file size. Set to <code>null</code> to remove the limit entirely. Existing objects that exceed the new limit are not affected.</td></tr>\n<tr><td>allowed_mime_types</td><td>string[]|null</td><td>Change the set of permitted MIME types. Set to <code>null</code> to allow any content type. Existing objects of disallowed types are not removed.</td></tr>\n</tbody></table>\n<p><strong>Response</strong> — Returns the updated bucket object.</p>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl -X PATCH storage.now/bucket/bk_a1b2c3 \\\n  -H &quot;Authorization: Bearer $TOKEN&quot; \\\n  -H &quot;Content-Type: application/json&quot; \\\n  -d '{&quot;public&quot;: false}'</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/bucket/bk_a1b2c3&quot;, {\n  method: &quot;PATCH&quot;,\n  headers: {\n    &quot;Authorization&quot;: `Bearer ${TOKEN}`,\n    &quot;Content-Type&quot;: &quot;application/json&quot;,\n  },\n  body: JSON.stringify({ public: false }),\n});</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{\n  &quot;id&quot;: &quot;bk_a1b2c3&quot;,\n  &quot;name&quot;: &quot;avatars&quot;,\n  &quot;public&quot;: false,\n  &quot;updated_at&quot;: 1710903600000\n}</code></pre>\n<h2 id=\"bucket-delete\"><span class=\"method method--delete\">DELETE</span> /bucket/:id</h2>\n<p>Deletes a bucket permanently. The bucket must be empty before it can be deleted — if the bucket still contains objects, the request fails with <code>409 Conflict</code>. Use <code>POST /bucket/:id/empty</code> to remove all objects first, then delete the bucket.</p>\n<p>Returns <code>{&quot;deleted&quot;: true}</code> on success.</p>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl -X DELETE storage.now/bucket/bk_a1b2c3 \\\n  -H &quot;Authorization: Bearer $TOKEN&quot;</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/bucket/bk_a1b2c3&quot;, {\n  method: &quot;DELETE&quot;,\n  headers: { &quot;Authorization&quot;: `Bearer ${TOKEN}` },\n});</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{&quot;deleted&quot;: true}</code></pre>\n<h2 id=\"bucket-empty\"><span class=\"method method--post\">POST</span> /bucket/:id/empty</h2>\n<p>Removes all objects from a bucket without deleting the bucket itself. This is a bulk operation — every object in the bucket is permanently deleted from both the database and the underlying object store. The bucket configuration (name, visibility, limits) remains intact.</p>\n<p>This operation cannot be undone. Consider creating signed download URLs for any objects you need to preserve before emptying the bucket.</p>\n<p>Returns <code>{&quot;emptied&quot;: true}</code> on success.</p>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl -X POST storage.now/bucket/bk_a1b2c3/empty \\\n  -H &quot;Authorization: Bearer $TOKEN&quot;</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/bucket/bk_a1b2c3/empty&quot;, {\n  method: &quot;POST&quot;,\n  headers: { &quot;Authorization&quot;: `Bearer ${TOKEN}` },\n});</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{&quot;emptied&quot;: true}</code></pre>\n<h1 id=\"objects\">Objects</h1>\n<p>Objects are the files stored within your buckets. Each object has a path that acts as its unique identifier within the bucket — paths use <code>/</code> as a conventional separator, and folders are implicit rather than explicit entities. For example, uploading to <code>reports/q1.pdf</code> does not require creating a <code>reports/</code> folder first.</p>\n<p>Content types are auto-detected from the file extension at upload time. You can override this by providing an explicit <code>Content-Type</code> header. Objects can be up to 100 MB per request; for larger files, use signed upload URLs which stream bytes directly to the object store.</p>\n<h2 id=\"object-upsert\"><span class=\"method method--put\">PUT</span> /object/:bucket/*path</h2>\n<p>Uploads a file to the specified path in a bucket. If an object already exists at that path, it is replaced with the new content and the <code>updated_at</code> timestamp is refreshed. If no object exists, a new one is created.</p>\n<p>The request body should contain the raw file bytes, not JSON. The server validates the upload against the bucket&#39;s <code>file_size_limit</code> and <code>allowed_mime_types</code> constraints — if either check fails, the request is rejected with <code>413 Too Large</code> or <code>415 Unsupported Media Type</code>.</p>\n<p>Returns the object metadata with <code>201 Created</code> for new objects or <code>200 OK</code> for replacements.</p>\n<p><strong>Path parameters</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>bucket</td><td>string</td><td>The name of the target bucket. Example: <code>&quot;documents&quot;</code>.</td></tr>\n<tr><td>*path</td><td>string</td><td>The full path for the object within the bucket, including any folder-like prefixes. Example: <code>&quot;reports/q1.pdf&quot;</code>.</td></tr>\n</tbody></table>\n<p><strong>Headers</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Header</th><th>Required</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>Content-Type</td><td>No</td><td>MIME type of the uploaded file. If omitted, the server infers the type from the file extension — <code>.pdf</code> becomes <code>application/pdf</code>, <code>.png</code> becomes <code>image/png</code>, and so on. Provide this header when the extension is ambiguous or missing.</td></tr>\n<tr><td>Content-Length</td><td>No</td><td>File size in bytes. Including this header allows the server to reject oversized uploads before reading the full body.</td></tr>\n</tbody></table>\n<p><strong>Response</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>id</td><td>string</td><td>Unique object identifier, prefixed with <code>o_</code>. Example: <code>&quot;o_x7y8z9&quot;</code>.</td></tr>\n<tr><td>bucket</td><td>string</td><td>Name of the bucket containing the object.</td></tr>\n<tr><td>path</td><td>string</td><td>Full object path within the bucket.</td></tr>\n<tr><td>name</td><td>string</td><td>The filename — the last segment of the path. For <code>&quot;reports/q1.pdf&quot;</code> this is <code>&quot;q1.pdf&quot;</code>.</td></tr>\n<tr><td>content_type</td><td>string</td><td>The resolved MIME type of the object.</td></tr>\n<tr><td>size</td><td>integer</td><td>File size in bytes.</td></tr>\n<tr><td>created_at</td><td>integer</td><td>Creation timestamp in milliseconds.</td></tr>\n</tbody></table>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl -X PUT storage.now/object/documents/reports/q1.pdf \\\n  -H &quot;Authorization: Bearer $TOKEN&quot; \\\n  -H &quot;Content-Type: application/pdf&quot; \\\n  -T report.pdf</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/object/documents/reports/q1.pdf&quot;, {\n  method: &quot;PUT&quot;,\n  headers: {\n    &quot;Authorization&quot;: `Bearer ${TOKEN}`,\n    &quot;Content-Type&quot;: &quot;application/pdf&quot;,\n  },\n  body: fileBuffer,\n});\nconst obj = await res.json();</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{\n  &quot;id&quot;: &quot;o_x7y8z9&quot;,\n  &quot;bucket&quot;: &quot;documents&quot;,\n  &quot;path&quot;: &quot;reports/q1.pdf&quot;,\n  &quot;name&quot;: &quot;q1.pdf&quot;,\n  &quot;content_type&quot;: &quot;application/pdf&quot;,\n  &quot;size&quot;: 524288,\n  &quot;created_at&quot;: 1710892800000\n}</code></pre>\n<h2 id=\"object-create\"><span class=\"method method--post\">POST</span> /object/:bucket/*path</h2>\n<p>Uploads a file, but only if no object already exists at the given path. If the path is already occupied, the request fails with <code>409 Conflict</code>. Use <code>PUT</code> instead if you want to overwrite existing objects.</p>\n<p>Accepts the same path parameters, headers, and request body as <code>PUT /object/:bucket/*path</code>. Always returns <code>201 Created</code> on success.</p>\n<h2 id=\"object-download\"><span class=\"method method--get\">GET</span> /object/:bucket/*path</h2>\n<p>Downloads an object as raw bytes. The response includes <code>Content-Type</code>, <code>Content-Length</code>, <code>ETag</code>, and <code>Last-Modified</code> headers for cache integration. Supports HTTP <code>Range</code> headers for partial downloads — useful for resumable transfers or streaming media.</p>\n<p>Add <code>?download</code> to the URL to force the browser to download the file instead of displaying it inline. You can optionally specify a custom filename: <code>?download=report-final.pdf</code>.</p>\n<p><strong>Path parameters</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>bucket</td><td>string</td><td>Bucket name.</td></tr>\n<tr><td>*path</td><td>string</td><td>Object path within the bucket.</td></tr>\n</tbody></table>\n<p><strong>Query parameters</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>download</td><td>string</td><td>When present, the response includes a <code>Content-Disposition: attachment</code> header. Optionally provide a value to set the download filename, like <code>?download=my-report.pdf</code>.</td></tr>\n</tbody></table>\n<p><strong>Response</strong> — Raw file bytes with appropriate HTTP headers. No JSON body.</p>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code># Download a file\ncurl storage.now/object/documents/reports/q1.pdf \\\n  -H &quot;Authorization: Bearer $TOKEN&quot; \\\n  -o q1.pdf\n\n# Force download with custom filename\ncurl &quot;storage.now/object/documents/reports/q1.pdf?download=Q1-Report.pdf&quot; \\\n  -H &quot;Authorization: Bearer $TOKEN&quot; \\\n  -o Q1-Report.pdf</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/object/documents/reports/q1.pdf&quot;, {\n  headers: { &quot;Authorization&quot;: `Bearer ${TOKEN}` },\n});\nconst blob = await res.blob();</code></pre>\n<h2 id=\"object-download-public\"><span class=\"method method--get\">GET</span> /object/public/:bucket/*path</h2>\n<p>Downloads an object from a public bucket. No authentication is required — this endpoint is designed for serving static assets, avatars, and any publicly accessible content directly to browsers or CDNs.</p>\n<p>If the bucket does not exist or is not marked as public, the request returns <code>404 Not Found</code>. Responses include a <code>Cache-Control: public, max-age=3600</code> header to encourage caching.</p>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl storage.now/object/public/avatars/alice.png -o alice.png</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>// In a browser, simply use the URL directly:\nconst img = document.createElement(&quot;img&quot;);\nimg.src = &quot;https://storage.now/object/public/avatars/alice.png&quot;;</code></pre>\n<h2 id=\"object-head\"><span class=\"method method--head\">HEAD</span> /object/:bucket/*path</h2>\n<p>Returns file metadata as HTTP response headers without transferring the file body. This is the most efficient way to check whether an object exists and inspect its size and type — no bytes are downloaded.</p>\n<p><strong>Response headers</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Header</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>Content-Type</td><td>The MIME type of the object.</td></tr>\n<tr><td>Content-Length</td><td>File size in bytes.</td></tr>\n<tr><td>Last-Modified</td><td>Timestamp of the last modification in HTTP date format.</td></tr>\n</tbody></table>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl -I storage.now/object/documents/reports/q1.pdf \\\n  -H &quot;Authorization: Bearer $TOKEN&quot;</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/object/documents/reports/q1.pdf&quot;, {\n  method: &quot;HEAD&quot;,\n  headers: { &quot;Authorization&quot;: `Bearer ${TOKEN}` },\n});\nconsole.log(res.headers.get(&quot;Content-Length&quot;)); // &quot;524288&quot;\nconsole.log(res.headers.get(&quot;Content-Type&quot;));   // &quot;application/pdf&quot;</code></pre>\n<h2 id=\"object-info\"><span class=\"method method--get\">GET</span> /object/info/:bucket/*path</h2>\n<p>Returns detailed metadata about an object as a JSON response. Unlike <code>HEAD</code>, which returns metadata in HTTP headers, this endpoint provides a structured JSON body that includes the object ID, custom metadata, and precise timestamps. Use this when you need the full object record for display or processing.</p>\n<p><strong>Response</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>id</td><td>string</td><td>Unique object identifier.</td></tr>\n<tr><td>bucket</td><td>string</td><td>Bucket name.</td></tr>\n<tr><td>path</td><td>string</td><td>Full object path.</td></tr>\n<tr><td>name</td><td>string</td><td>Filename (last path segment).</td></tr>\n<tr><td>content_type</td><td>string</td><td>MIME type.</td></tr>\n<tr><td>size</td><td>integer</td><td>File size in bytes.</td></tr>\n<tr><td>metadata</td><td>object</td><td>Custom key-value metadata attached to the object. Defaults to <code>{}</code>.</td></tr>\n<tr><td>created_at</td><td>integer</td><td>Creation timestamp in milliseconds.</td></tr>\n<tr><td>updated_at</td><td>integer</td><td>Last modification timestamp in milliseconds.</td></tr>\n<tr><td>accessed_at</td><td>integer|null</td><td>Last download timestamp in milliseconds, or <code>null</code> if never accessed.</td></tr>\n</tbody></table>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl storage.now/object/info/documents/reports/q1.pdf \\\n  -H &quot;Authorization: Bearer $TOKEN&quot;</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/object/info/documents/reports/q1.pdf&quot;, {\n  headers: { &quot;Authorization&quot;: `Bearer ${TOKEN}` },\n});\nconst info = await res.json();</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{\n  &quot;id&quot;: &quot;o_x7y8z9&quot;,\n  &quot;bucket&quot;: &quot;documents&quot;,\n  &quot;path&quot;: &quot;reports/q1.pdf&quot;,\n  &quot;name&quot;: &quot;q1.pdf&quot;,\n  &quot;content_type&quot;: &quot;application/pdf&quot;,\n  &quot;size&quot;: 524288,\n  &quot;metadata&quot;: {},\n  &quot;created_at&quot;: 1710892800000,\n  &quot;updated_at&quot;: 1710892800000,\n  &quot;accessed_at&quot;: 1710903600000\n}</code></pre>\n<h2 id=\"object-list\"><span class=\"method method--post\">POST</span> /object/list/:bucket</h2>\n<p>Lists objects within a bucket, with support for prefix filtering, text search, pagination, and custom sort order. This is the primary way to browse bucket contents, build file managers, or enumerate objects for batch processing.</p>\n<p>The <code>prefix</code> parameter acts like a directory filter — setting it to <code>&quot;reports/&quot;</code> returns only objects whose paths start with that prefix. Combine it with <code>search</code> to find objects by filename within a specific directory.</p>\n<p>Results are paginated with <code>limit</code> and <code>offset</code>. The default sort is ascending by name, but you can sort by <code>size</code>, <code>created_at</code>, or <code>updated_at</code> in either direction.</p>\n<p><strong>Path parameters</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>bucket</td><td>string</td><td>The name of the bucket to list.</td></tr>\n</tbody></table>\n<p><strong>Request body</strong> — All fields are optional.</p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Default</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>prefix</td><td>string</td><td><code>&quot;&quot;</code></td><td>Filter objects whose path starts with this string. Use trailing slashes to scope to a &quot;folder&quot;, like <code>&quot;reports/&quot;</code> or <code>&quot;images/thumbnails/&quot;</code>.</td></tr>\n<tr><td>search</td><td>string</td><td><code>&quot;&quot;</code></td><td>Free-text search term matched against object filenames. For example, <code>&quot;q1&quot;</code> matches both <code>&quot;q1.pdf&quot;</code> and <code>&quot;q1-summary.md&quot;</code>.</td></tr>\n<tr><td>limit</td><td>integer</td><td><code>100</code></td><td>Maximum number of objects to return. Must be between 1 and 1000.</td></tr>\n<tr><td>offset</td><td>integer</td><td><code>0</code></td><td>Number of objects to skip before starting to return results. Use with <code>limit</code> for pagination.</td></tr>\n<tr><td>sort_by</td><td>object</td><td><code>{&quot;column&quot;:&quot;name&quot;,&quot;order&quot;:&quot;asc&quot;}</code></td><td>Controls the sort order of results.</td></tr>\n<tr><td>sort_by.column</td><td>string</td><td><code>&quot;name&quot;</code></td><td>Which field to sort by. One of: <code>name</code>, <code>size</code>, <code>created_at</code>, <code>updated_at</code>.</td></tr>\n<tr><td>sort_by.order</td><td>string</td><td><code>&quot;asc&quot;</code></td><td>Sort direction. Either <code>&quot;asc&quot;</code> for ascending or <code>&quot;desc&quot;</code> for descending.</td></tr>\n</tbody></table>\n<p><strong>Response</strong> — Array of object metadata.</p>\n<table class=\"ref-table\">\n<thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>id</td><td>string</td><td>Object ID.</td></tr>\n<tr><td>name</td><td>string</td><td>Filename.</td></tr>\n<tr><td>path</td><td>string</td><td>Full path within the bucket.</td></tr>\n<tr><td>content_type</td><td>string</td><td>MIME type.</td></tr>\n<tr><td>size</td><td>integer</td><td>File size in bytes.</td></tr>\n<tr><td>metadata</td><td>object</td><td>Custom metadata key-value pairs.</td></tr>\n<tr><td>created_at</td><td>integer</td><td>Creation timestamp in milliseconds.</td></tr>\n<tr><td>updated_at</td><td>integer</td><td>Last modification timestamp in milliseconds.</td></tr>\n</tbody></table>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl -X POST storage.now/object/list/documents \\\n  -H &quot;Authorization: Bearer $TOKEN&quot; \\\n  -H &quot;Content-Type: application/json&quot; \\\n  -d '{\n    &quot;prefix&quot;: &quot;reports/&quot;,\n    &quot;search&quot;: &quot;q1&quot;,\n    &quot;sort_by&quot;: {&quot;column&quot;: &quot;name&quot;, &quot;order&quot;: &quot;asc&quot;},\n    &quot;limit&quot;: 50\n  }'</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/object/list/documents&quot;, {\n  method: &quot;POST&quot;,\n  headers: {\n    &quot;Authorization&quot;: `Bearer ${TOKEN}`,\n    &quot;Content-Type&quot;: &quot;application/json&quot;,\n  },\n  body: JSON.stringify({\n    prefix: &quot;reports/&quot;,\n    limit: 50,\n    sort_by: { column: &quot;updated_at&quot;, order: &quot;desc&quot; },\n  }),\n});\nconst objects = await res.json();</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>[\n  {&quot;id&quot;: &quot;o_a1b2&quot;, &quot;name&quot;: &quot;q1.pdf&quot;, &quot;path&quot;: &quot;reports/q1.pdf&quot;, &quot;size&quot;: 524288, &quot;content_type&quot;: &quot;application/pdf&quot;, &quot;created_at&quot;: 1710892800000},\n  {&quot;id&quot;: &quot;o_c3d4&quot;, &quot;name&quot;: &quot;q1-summary.md&quot;, &quot;path&quot;: &quot;reports/q1-summary.md&quot;, &quot;size&quot;: 2048, &quot;content_type&quot;: &quot;text/markdown&quot;, &quot;created_at&quot;: 1710893000000}\n]</code></pre>\n<h2 id=\"object-delete\"><span class=\"method method--delete\">DELETE</span> /object/:bucket</h2>\n<p>Deletes one or more objects from a bucket in a single request. Provide an array of paths to remove — up to 100 per request. Objects are permanently deleted from both the database and the underlying object store.</p>\n<p>Paths that do not match any existing object are silently ignored and included in the <code>deleted</code> response array. This makes the operation idempotent — calling it twice with the same paths produces the same result.</p>\n<p><strong>Path parameters</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>bucket</td><td>string</td><td>The name of the bucket containing the objects.</td></tr>\n</tbody></table>\n<p><strong>Request body</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>paths</td><td>string[]</td><td>Yes</td><td>Array of object paths to delete. Maximum 100 paths per request. Example: <code>[&quot;reports/q1.pdf&quot;, &quot;reports/q1-summary.md&quot;]</code>.</td></tr>\n</tbody></table>\n<p><strong>Response</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>deleted</td><td>string[]</td><td>Array of paths that were deleted.</td></tr>\n</tbody></table>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl -X DELETE storage.now/object/documents \\\n  -H &quot;Authorization: Bearer $TOKEN&quot; \\\n  -H &quot;Content-Type: application/json&quot; \\\n  -d '{&quot;paths&quot;: [&quot;reports/q1.pdf&quot;, &quot;reports/q1-summary.md&quot;]}'</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/object/documents&quot;, {\n  method: &quot;DELETE&quot;,\n  headers: {\n    &quot;Authorization&quot;: `Bearer ${TOKEN}`,\n    &quot;Content-Type&quot;: &quot;application/json&quot;,\n  },\n  body: JSON.stringify({\n    paths: [&quot;reports/q1.pdf&quot;, &quot;reports/q1-summary.md&quot;],\n  }),\n});\nconst result = await res.json();</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{&quot;deleted&quot;: [&quot;reports/q1.pdf&quot;, &quot;reports/q1-summary.md&quot;]}</code></pre>\n<h2 id=\"object-move\"><span class=\"method method--post\">POST</span> /object/move</h2>\n<p>Moves or renames an object within a bucket. The object&#39;s content is preserved — only the path changes. If an object already exists at the destination path, the request fails with <code>409 Conflict</code>.</p>\n<p>This is a metadata-only operation and does not copy the underlying file data, so it completes instantly regardless of file size.</p>\n<p><strong>Request body</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>bucket</td><td>string</td><td>Yes</td><td>The name of the bucket containing the object.</td></tr>\n<tr><td>from</td><td>string</td><td>Yes</td><td>The current path of the object. Example: <code>&quot;reports/q1.pdf&quot;</code>.</td></tr>\n<tr><td>to</td><td>string</td><td>Yes</td><td>The desired new path. Example: <code>&quot;archive/2025/q1.pdf&quot;</code>.</td></tr>\n</tbody></table>\n<p><strong>Response</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>path</td><td>string</td><td>The new path of the object after the move.</td></tr>\n</tbody></table>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl -X POST storage.now/object/move \\\n  -H &quot;Authorization: Bearer $TOKEN&quot; \\\n  -H &quot;Content-Type: application/json&quot; \\\n  -d '{&quot;bucket&quot;: &quot;documents&quot;, &quot;from&quot;: &quot;reports/q1.pdf&quot;, &quot;to&quot;: &quot;archive/2025/q1.pdf&quot;}'</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/object/move&quot;, {\n  method: &quot;POST&quot;,\n  headers: {\n    &quot;Authorization&quot;: `Bearer ${TOKEN}`,\n    &quot;Content-Type&quot;: &quot;application/json&quot;,\n  },\n  body: JSON.stringify({\n    bucket: &quot;documents&quot;,\n    from: &quot;reports/q1.pdf&quot;,\n    to: &quot;archive/2025/q1.pdf&quot;,\n  }),\n});</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{&quot;path&quot;: &quot;archive/2025/q1.pdf&quot;}</code></pre>\n<h2 id=\"object-copy\"><span class=\"method method--post\">POST</span> /object/copy</h2>\n<p>Creates a copy of an object, optionally across different buckets. Both the source and destination buckets must belong to the authenticated actor. A new object ID is generated for the copy, and the file content is duplicated in the object store.</p>\n<p>If an object already exists at the destination path, the request fails with <code>409 Conflict</code>.</p>\n<p><strong>Request body</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>from_bucket</td><td>string</td><td>Yes</td><td>Name of the source bucket.</td></tr>\n<tr><td>from_path</td><td>string</td><td>Yes</td><td>Path of the object to copy.</td></tr>\n<tr><td>to_bucket</td><td>string</td><td>Yes</td><td>Name of the destination bucket. Can be the same as <code>from_bucket</code> for an in-bucket copy.</td></tr>\n<tr><td>to_path</td><td>string</td><td>Yes</td><td>Destination path for the new copy.</td></tr>\n</tbody></table>\n<p><strong>Response</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>id</td><td>string</td><td>The new object ID for the copy.</td></tr>\n<tr><td>path</td><td>string</td><td>Destination path.</td></tr>\n</tbody></table>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl -X POST storage.now/object/copy \\\n  -H &quot;Authorization: Bearer $TOKEN&quot; \\\n  -H &quot;Content-Type: application/json&quot; \\\n  -d '{\n    &quot;from_bucket&quot;: &quot;documents&quot;,\n    &quot;from_path&quot;: &quot;reports/q1.pdf&quot;,\n    &quot;to_bucket&quot;: &quot;archive&quot;,\n    &quot;to_path&quot;: &quot;2025/q1.pdf&quot;\n  }'</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/object/copy&quot;, {\n  method: &quot;POST&quot;,\n  headers: {\n    &quot;Authorization&quot;: `Bearer ${TOKEN}`,\n    &quot;Content-Type&quot;: &quot;application/json&quot;,\n  },\n  body: JSON.stringify({\n    from_bucket: &quot;documents&quot;,\n    from_path: &quot;reports/q1.pdf&quot;,\n    to_bucket: &quot;archive&quot;,\n    to_path: &quot;2025/q1.pdf&quot;,\n  }),\n});</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{&quot;id&quot;: &quot;o_e5f6g7&quot;, &quot;path&quot;: &quot;2025/q1.pdf&quot;}</code></pre>\n<h1 id=\"signed-urls\">Signed URLs</h1>\n<p>Signed URLs provide time-limited access to private objects without requiring the recipient to authenticate. They are the universal sharing primitive — one mechanism that covers three common scenarios:</p>\n<ol>\n<li><strong>Sharing a private file</strong> — Generate a signed download URL and send it to anyone. They can download the file until the URL expires.</li>\n<li><strong>Accepting uploads without exposing credentials</strong> — Generate a signed upload URL and give it to a client. They can upload exactly one file to the specified path.</li>\n<li><strong>Temporary public access</strong> — Signed URLs work like pre-authenticated links with an automatic expiration, so you never need to toggle a bucket&#39;s visibility.</li>\n</ol>\n<p>Signed download URLs are reusable — the same URL can be accessed multiple times until it expires. Signed upload URLs are single-use — once a file is uploaded through the URL, it is consumed and cannot be used again.</p>\n<h2 id=\"sign-create\"><span class=\"method method--post\">POST</span> /object/sign/:bucket</h2>\n<p>Creates one or more signed download URLs for objects in a bucket. You can sign a single path or batch-sign up to 100 paths in one request. Each signed URL grants unauthenticated read access to the object for the specified duration.</p>\n<p>The default expiration is 1 hour (3600 seconds). You can set a custom expiration up to 7 days (604800 seconds). After expiration, the signed URL returns <code>403 Forbidden</code>.</p>\n<p><strong>Path parameters</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>bucket</td><td>string</td><td>Name of the bucket containing the objects.</td></tr>\n</tbody></table>\n<p><strong>Request body — single path</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>path</td><td>string</td><td>Yes*</td><td>Object path to sign. Provide either <code>path</code> for a single URL or <code>paths</code> for a batch — not both.</td></tr>\n<tr><td>expires_in</td><td>integer</td><td>No</td><td>How long the URL remains valid, in seconds. Defaults to <code>3600</code> (1 hour). Maximum <code>604800</code> (7 days).</td></tr>\n</tbody></table>\n<p><strong>Request body — batch</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>paths</td><td>string[]</td><td>Yes*</td><td>Array of object paths to sign. Up to 100 paths per request.</td></tr>\n<tr><td>expires_in</td><td>integer</td><td>No</td><td>Expiry duration in seconds, applied to all URLs in the batch.</td></tr>\n</tbody></table>\n<p><strong>Response — single</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>signed_url</td><td>string</td><td>Relative URL for downloading the object. Example: <code>&quot;/sign/tok_abc123&quot;</code>.</td></tr>\n<tr><td>token</td><td>string</td><td>The signed URL token, which can be used to construct the full URL.</td></tr>\n<tr><td>path</td><td>string</td><td>The object path that was signed.</td></tr>\n<tr><td>expires_at</td><td>integer</td><td>Expiration timestamp in milliseconds.</td></tr>\n</tbody></table>\n<p><strong>Response — batch</strong> — Array of signed URL objects, one per path.</p>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code># Sign a single path\ncurl -X POST storage.now/object/sign/documents \\\n  -H &quot;Authorization: Bearer $TOKEN&quot; \\\n  -H &quot;Content-Type: application/json&quot; \\\n  -d '{&quot;path&quot;: &quot;reports/q1.pdf&quot;, &quot;expires_in&quot;: 3600}'</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/object/sign/documents&quot;, {\n  method: &quot;POST&quot;,\n  headers: {\n    &quot;Authorization&quot;: `Bearer ${TOKEN}`,\n    &quot;Content-Type&quot;: &quot;application/json&quot;,\n  },\n  body: JSON.stringify({\n    path: &quot;reports/q1.pdf&quot;,\n    expires_in: 3600,\n  }),\n});\nconst signed = await res.json();</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{\n  &quot;signed_url&quot;: &quot;/sign/tok_abc123&quot;,\n  &quot;token&quot;: &quot;tok_abc123&quot;,\n  &quot;path&quot;: &quot;reports/q1.pdf&quot;,\n  &quot;expires_at&quot;: 1710896400000\n}</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code># Batch sign multiple paths\ncurl -X POST storage.now/object/sign/documents \\\n  -H &quot;Authorization: Bearer $TOKEN&quot; \\\n  -H &quot;Content-Type: application/json&quot; \\\n  -d '{&quot;paths&quot;: [&quot;reports/q1.pdf&quot;, &quot;reports/q2.pdf&quot;], &quot;expires_in&quot;: 3600}'</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>[\n  {&quot;signed_url&quot;: &quot;/sign/tok_abc&quot;, &quot;path&quot;: &quot;reports/q1.pdf&quot;, &quot;token&quot;: &quot;tok_abc&quot;, &quot;expires_at&quot;: 1710896400000},\n  {&quot;signed_url&quot;: &quot;/sign/tok_def&quot;, &quot;path&quot;: &quot;reports/q2.pdf&quot;, &quot;token&quot;: &quot;tok_def&quot;, &quot;expires_at&quot;: 1710896400000}\n]</code></pre>\n<h2 id=\"sign-upload-create\"><span class=\"method method--post\">POST</span> /object/upload/sign/:bucket/*path</h2>\n<p>Creates a signed upload URL that allows anyone to upload a single file to the specified path without authentication. This is ideal for accepting file uploads from untrusted clients — for example, letting users upload profile pictures directly from a browser without exposing your API key.</p>\n<p>The signed upload URL expires after 1 hour by default. Once a file is uploaded through the URL, the URL is consumed and cannot be reused.</p>\n<p><strong>Path parameters</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>bucket</td><td>string</td><td>Name of the target bucket.</td></tr>\n<tr><td>*path</td><td>string</td><td>The exact path where the uploaded object will be stored. Example: <code>&quot;inbox/photo.jpg&quot;</code>.</td></tr>\n</tbody></table>\n<p><strong>Response</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>signed_url</td><td>string</td><td>Relative URL for uploading. Example: <code>&quot;/upload/sign/tok_xyz&quot;</code>.</td></tr>\n<tr><td>token</td><td>string</td><td>The signed URL token.</td></tr>\n<tr><td>path</td><td>string</td><td>Target object path.</td></tr>\n<tr><td>expires_at</td><td>integer</td><td>Expiration timestamp in milliseconds.</td></tr>\n</tbody></table>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl -X POST storage.now/object/upload/sign/documents/inbox/upload.pdf \\\n  -H &quot;Authorization: Bearer $TOKEN&quot;</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(\n  &quot;https://storage.now/object/upload/sign/documents/inbox/upload.pdf&quot;,\n  {\n    method: &quot;POST&quot;,\n    headers: { &quot;Authorization&quot;: `Bearer ${TOKEN}` },\n  }\n);\nconst { signed_url, token } = await res.json();</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{\n  &quot;signed_url&quot;: &quot;/upload/sign/tok_xyz&quot;,\n  &quot;token&quot;: &quot;tok_xyz&quot;,\n  &quot;path&quot;: &quot;inbox/upload.pdf&quot;,\n  &quot;expires_at&quot;: 1710896400000\n}</code></pre>\n<h2 id=\"sign-download\"><span class=\"method method--get\">GET</span> /sign/:token</h2>\n<p>Downloads an object using a signed URL. No authentication required — the token embedded in the URL serves as the access credential. This is the endpoint your users visit when they click a shared link.</p>\n<p>If the signed URL has expired, the response is <code>403 Forbidden</code> with an error message indicating the expiration. If the token does not exist, the response is <code>404 Not Found</code>.</p>\n<p><strong>Query parameters</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>download</td><td>string</td><td>When present, adds <code>Content-Disposition: attachment</code> to the response, prompting the browser to download the file. Provide a value to suggest a filename, like <code>?download=report.pdf</code>.</td></tr>\n</tbody></table>\n<p><strong>Response</strong> — Raw file bytes with <code>Content-Type</code>, <code>Content-Length</code>, and <code>ETag</code> headers.</p>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl storage.now/sign/tok_abc123 -o report.pdf\n\n# Force download with custom filename\ncurl &quot;storage.now/sign/tok_abc123?download=Q1-Financial-Report.pdf&quot; -o report.pdf</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/sign/tok_abc123&quot;);\nconst blob = await res.blob();\nconst url = URL.createObjectURL(blob);</code></pre>\n<h2 id=\"sign-upload\"><span class=\"method method--put\">PUT</span> /upload/sign/:token</h2>\n<p>Uploads a file using a signed upload URL. No authentication required — the token in the URL authorizes the upload. The file is stored at the path that was specified when the signed URL was created.</p>\n<p>The signed URL is consumed after a successful upload and cannot be reused. If the URL has expired, the response is <code>403 Forbidden</code>. If the token does not exist or has already been used, the response is <code>404 Not Found</code>.</p>\n<p><strong>Headers</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Header</th><th>Required</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>Content-Type</td><td>No</td><td>MIME type of the uploaded file. If omitted, the type is inferred from the target path extension.</td></tr>\n</tbody></table>\n<p><strong>Response</strong> — <code>201 Created</code> with the newly created object metadata.</p>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl -X PUT storage.now/upload/sign/tok_xyz \\\n  -H &quot;Content-Type: application/pdf&quot; \\\n  -T file.pdf</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/upload/sign/tok_xyz&quot;, {\n  method: &quot;PUT&quot;,\n  headers: { &quot;Content-Type&quot;: &quot;application/pdf&quot; },\n  body: fileBuffer,\n});\nconst obj = await res.json();</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{\n  &quot;id&quot;: &quot;o_h8i9j0&quot;,\n  &quot;bucket&quot;: &quot;documents&quot;,\n  &quot;path&quot;: &quot;inbox/upload.pdf&quot;,\n  &quot;size&quot;: 1048576,\n  &quot;content_type&quot;: &quot;application/pdf&quot;,\n  &quot;created_at&quot;: 1710892800000\n}</code></pre>\n<h1 id=\"auth\">Auth</h1>\n<p>Identity and session management. The auth system supports two methods: Ed25519 challenge-response for machines and magic links for humans. Both methods produce Bearer tokens that grant access to all authenticated API endpoints.</p>\n<p>Agent identities use the <code>a/name</code> format and authenticate with Ed25519 key pairs — ideal for bots, scripts, and CI/CD pipelines. Human identities use the <code>u/email</code> format and authenticate via email-based magic links — no passwords needed.</p>\n<h2 id=\"auth-register\"><span class=\"method method--post\">POST</span> /auth/register</h2>\n<p>Creates a new actor identity. Every interaction with the API is associated with an actor — this is how you establish one. Agents must provide an Ed25519 public key at registration time; humans must provide an email address.</p>\n<p>If an actor with the same identifier already exists, the request fails with <code>409 Conflict</code>. Actor identifiers are permanent and cannot be changed after creation.</p>\n<p><strong>Request body</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>actor</td><td>string</td><td>Yes</td><td>Unique actor identifier. Use <code>a/name</code> for agents (e.g. <code>&quot;a/deploy-bot&quot;</code>) or <code>u/email</code> for humans (e.g. <code>&quot;u/alice@example.com&quot;</code>). The prefix determines the actor type.</td></tr>\n<tr><td>type</td><td>string</td><td>No</td><td>Explicitly set the actor type: <code>&quot;agent&quot;</code> or <code>&quot;human&quot;</code>. Defaults to <code>&quot;agent&quot;</code> if the identifier starts with <code>a/</code>, or <code>&quot;human&quot;</code> if it starts with <code>u/</code>.</td></tr>\n<tr><td>public_key</td><td>string</td><td>Yes*</td><td>Base64url-encoded Ed25519 public key. Required for agent actors. This key is used to verify challenge signatures during authentication.</td></tr>\n<tr><td>email</td><td>string</td><td>Yes*</td><td>Email address. Required for human actors. Magic link emails are sent to this address.</td></tr>\n</tbody></table>\n<p><strong>Response</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>actor</td><td>string</td><td>The registered actor identifier.</td></tr>\n<tr><td>created_at</td><td>integer</td><td>Registration timestamp in milliseconds.</td></tr>\n</tbody></table>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl -X POST storage.now/auth/register \\\n  -H &quot;Content-Type: application/json&quot; \\\n  -d '{&quot;actor&quot;: &quot;a/deploy-bot&quot;, &quot;public_key&quot;: &quot;&lt;base64url-ed25519&gt;&quot;}'</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/auth/register&quot;, {\n  method: &quot;POST&quot;,\n  headers: { &quot;Content-Type&quot;: &quot;application/json&quot; },\n  body: JSON.stringify({\n    actor: &quot;a/deploy-bot&quot;,\n    public_key: publicKeyBase64url,\n  }),\n});</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{&quot;actor&quot;: &quot;a/deploy-bot&quot;, &quot;created_at&quot;: 1710892800000}</code></pre>\n<h2 id=\"auth-challenge\"><span class=\"method method--post\">POST</span> /auth/challenge</h2>\n<p>Requests an authentication challenge for Ed25519 key-based login. The server returns a random nonce that must be signed with the actor&#39;s private key and submitted to <code>POST /auth/verify</code> within 5 minutes.</p>\n<p>This is the first step in the two-step Ed25519 authentication flow. The challenge proves that the client possesses the private key corresponding to the public key registered for the actor.</p>\n<p><strong>Request body</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>actor</td><td>string</td><td>Yes</td><td>The actor identifier to authenticate as. Must be a registered agent actor.</td></tr>\n</tbody></table>\n<p><strong>Response</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>challenge_id</td><td>string</td><td>Unique challenge identifier, prefixed with <code>ch_</code>. Pass this to the verify step.</td></tr>\n<tr><td>nonce</td><td>string</td><td>Random string to sign with your Ed25519 private key.</td></tr>\n<tr><td>expires_at</td><td>integer</td><td>Challenge expiration timestamp in milliseconds. Challenges expire after 5 minutes.</td></tr>\n</tbody></table>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl -X POST storage.now/auth/challenge \\\n  -H &quot;Content-Type: application/json&quot; \\\n  -d '{&quot;actor&quot;: &quot;a/deploy-bot&quot;}'</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/auth/challenge&quot;, {\n  method: &quot;POST&quot;,\n  headers: { &quot;Content-Type&quot;: &quot;application/json&quot; },\n  body: JSON.stringify({ actor: &quot;a/deploy-bot&quot; }),\n});\nconst { challenge_id, nonce } = await res.json();\n// Sign the nonce with your Ed25519 private key</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{\n  &quot;challenge_id&quot;: &quot;ch_k4m5n6&quot;,\n  &quot;nonce&quot;: &quot;a1b2c3d4e5f6...&quot;,\n  &quot;expires_at&quot;: 1710893100000\n}</code></pre>\n<h2 id=\"auth-verify\"><span class=\"method method--post\">POST</span> /auth/verify</h2>\n<p>Verifies an Ed25519 signature against a previously issued challenge and returns an access token on success. The signature must be the base64url-encoded result of signing the challenge nonce with the actor&#39;s Ed25519 private key.</p>\n<p>Access tokens expire after 2 hours. After expiration, request a new challenge and verify again to obtain a fresh token.</p>\n<p><strong>Request body</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>challenge_id</td><td>string</td><td>Yes</td><td>The challenge ID returned by <code>POST /auth/challenge</code>.</td></tr>\n<tr><td>actor</td><td>string</td><td>Yes</td><td>The actor identifier. Must match the actor from the challenge request.</td></tr>\n<tr><td>signature</td><td>string</td><td>Yes</td><td>Base64url-encoded Ed25519 signature of the challenge nonce.</td></tr>\n</tbody></table>\n<p><strong>Response</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>access_token</td><td>string</td><td>Bearer token for authenticating subsequent API requests.</td></tr>\n<tr><td>expires_at</td><td>integer</td><td>Token expiration timestamp in milliseconds. Tokens are valid for 2 hours.</td></tr>\n</tbody></table>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl -X POST storage.now/auth/verify \\\n  -H &quot;Content-Type: application/json&quot; \\\n  -d '{&quot;challenge_id&quot;: &quot;ch_k4m5n6&quot;, &quot;actor&quot;: &quot;a/deploy-bot&quot;, &quot;signature&quot;: &quot;&lt;base64url&gt;&quot;}'</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/auth/verify&quot;, {\n  method: &quot;POST&quot;,\n  headers: { &quot;Content-Type&quot;: &quot;application/json&quot; },\n  body: JSON.stringify({\n    challenge_id: challenge_id,\n    actor: &quot;a/deploy-bot&quot;,\n    signature: signatureBase64url,\n  }),\n});\nconst { access_token } = await res.json();</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{&quot;access_token&quot;: &quot;tok_session_...&quot;, &quot;expires_at&quot;: 1710900000000}</code></pre>\n<h2 id=\"auth-magic-link\"><span class=\"method method--post\">POST</span> /auth/magic-link</h2>\n<p>Sends a magic link email for passwordless human authentication. The email contains a one-time link that, when clicked, creates a session and redirects to the storage browser. Magic links expire after 15 minutes.</p>\n<p>Rate limited to 5 requests per minute per IP to prevent email abuse.</p>\n<p><strong>Request body</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>email</td><td>string</td><td>Yes</td><td>Email address of the human actor. Must match a registered <code>u/email</code> actor.</td></tr>\n</tbody></table>\n<p><strong>Response</strong> — <code>202 Accepted</code>.</p>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl -X POST storage.now/auth/magic-link \\\n  -H &quot;Content-Type: application/json&quot; \\\n  -d '{&quot;email&quot;: &quot;alice@example.com&quot;}'</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{&quot;sent&quot;: true}</code></pre>\n<h2 id=\"auth-magic-verify\"><span class=\"method method--get\">GET</span> /auth/magic/:token</h2>\n<p>Verifies a magic link token from an email click. On success, sets a <code>session</code> cookie with <code>HttpOnly</code> and <code>Secure</code> flags, then redirects to <code>/browse</code>. This endpoint is not typically called directly — it is the target of the link sent by <code>POST /auth/magic-link</code>.</p>\n<p>If the token is invalid or expired, the response is <code>401 Unauthorized</code>.</p>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>GET /auth/magic/mtk_abc123\n\n→ 302 Found\nSet-Cookie: session=...; Path=/; HttpOnly; Secure\nLocation: /browse</code></pre>\n<h2 id=\"auth-logout\"><span class=\"method method--post\">POST</span> /auth/logout</h2>\n<p>Ends the current session by invalidating the Bearer token or session cookie. After logout, the token can no longer be used for authentication and any subsequent requests with it will receive <code>401 Unauthorized</code>.</p>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl -X POST storage.now/auth/logout \\\n  -H &quot;Authorization: Bearer $TOKEN&quot;</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>await fetch(&quot;https://storage.now/auth/logout&quot;, {\n  method: &quot;POST&quot;,\n  headers: { &quot;Authorization&quot;: `Bearer ${TOKEN}` },\n});</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{&quot;ok&quot;: true}</code></pre>\n<h1 id=\"keys\">Keys</h1>\n<p>Scoped API keys for long-lived programmatic access. Unlike session tokens that expire after 2 hours, API keys can last indefinitely or until a specified expiration date. They are ideal for CI/CD pipelines, cron jobs, and third-party integrations that need persistent access.</p>\n<p>Each key can be scoped to specific operations (<code>bucket:read</code>, <code>object:write</code>, etc.) and restricted to a path prefix so it only has access to a subset of your objects. Key tokens are prefixed with <code>sk_</code> and can be used as Bearer tokens in the <code>Authorization</code> header, just like session tokens.</p>\n<h2 id=\"key-create\"><span class=\"method method--post\">POST</span> /keys</h2>\n<p>Creates a new API key. The key token is returned exactly once in the response — store it securely, because it cannot be retrieved again. If you lose the token, you must revoke the key and create a new one.</p>\n<p><strong>Request body</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Required</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>name</td><td>string</td><td>Yes</td><td>A human-readable label for the key. Use something descriptive like <code>&quot;ci-deploy&quot;</code>, <code>&quot;backup-cronjob&quot;</code>, or <code>&quot;frontend-uploads&quot;</code>.</td></tr>\n<tr><td>scopes</td><td>string</td><td>No</td><td>Comma-separated list of permission scopes. Defaults to <code>&quot;*&quot;</code> (all permissions). Available scopes: <code>bucket:read</code>, <code>bucket:write</code>, <code>object:read</code>, <code>object:write</code>.</td></tr>\n<tr><td>path_prefix</td><td>string</td><td>No</td><td>Restrict the key to objects whose path starts with this prefix. For example, <code>&quot;uploads/&quot;</code> limits the key to only interact with objects under the <code>uploads/</code> directory. Defaults to <code>&quot;&quot;</code> (all paths).</td></tr>\n<tr><td>expires_at</td><td>integer</td><td>No</td><td>Expiration timestamp in milliseconds. After this time, the key is automatically invalidated. Omit or set to <code>null</code> for a key that never expires.</td></tr>\n</tbody></table>\n<p><strong>Response</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>id</td><td>string</td><td>Unique key identifier, prefixed with <code>ak_</code>.</td></tr>\n<tr><td>name</td><td>string</td><td>The key name as provided.</td></tr>\n<tr><td>token</td><td>string</td><td>The API key token, prefixed with <code>sk_</code>. <strong>Shown once — store it securely.</strong></td></tr>\n<tr><td>scopes</td><td>string</td><td>The granted scopes.</td></tr>\n<tr><td>created_at</td><td>integer</td><td>Creation timestamp in milliseconds.</td></tr>\n</tbody></table>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl -X POST storage.now/keys \\\n  -H &quot;Authorization: Bearer $TOKEN&quot; \\\n  -H &quot;Content-Type: application/json&quot; \\\n  -d '{&quot;name&quot;: &quot;ci-deploy&quot;, &quot;scopes&quot;: &quot;object:read,object:write&quot;}'</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/keys&quot;, {\n  method: &quot;POST&quot;,\n  headers: {\n    &quot;Authorization&quot;: `Bearer ${TOKEN}`,\n    &quot;Content-Type&quot;: &quot;application/json&quot;,\n  },\n  body: JSON.stringify({\n    name: &quot;ci-deploy&quot;,\n    scopes: &quot;object:read,object:write&quot;,\n  }),\n});\nconst { token } = await res.json();\n// Store token securely — it cannot be retrieved again</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{\n  &quot;id&quot;: &quot;ak_abc&quot;,\n  &quot;name&quot;: &quot;ci-deploy&quot;,\n  &quot;token&quot;: &quot;sk_live_a1b2c3d4e5f6...&quot;,\n  &quot;scopes&quot;: &quot;object:read,object:write&quot;,\n  &quot;created_at&quot;: 1710892800000\n}</code></pre>\n<p>Use the token as: <code>Authorization: Bearer sk_live_a1b2c3d4e5f6...</code></p>\n<h2 id=\"key-list\"><span class=\"method method--get\">GET</span> /keys</h2>\n<p>Returns a list of all API keys belonging to the authenticated actor. For security, key tokens are never included in the response — only metadata such as the key name, scopes, and creation date. Use this to audit your active keys or find the ID of a key you want to revoke.</p>\n<p><strong>Response</strong> — Array of key objects (without tokens).</p>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl storage.now/keys \\\n  -H &quot;Authorization: Bearer $TOKEN&quot;</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/keys&quot;, {\n  headers: { &quot;Authorization&quot;: `Bearer ${TOKEN}` },\n});\nconst keys = await res.json();</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>[\n  {&quot;id&quot;: &quot;ak_abc&quot;, &quot;name&quot;: &quot;ci-deploy&quot;, &quot;scopes&quot;: &quot;object:read,object:write&quot;, &quot;created_at&quot;: 1710892800000},\n  {&quot;id&quot;: &quot;ak_def&quot;, &quot;name&quot;: &quot;backup&quot;, &quot;scopes&quot;: &quot;*&quot;, &quot;created_at&quot;: 1710893000000}\n]</code></pre>\n<h2 id=\"key-revoke\"><span class=\"method method--delete\">DELETE</span> /keys/:id</h2>\n<p>Revokes an API key immediately. Any requests using the revoked key&#39;s token will receive <code>401 Unauthorized</code> from the moment of revocation. This operation is permanent — a revoked key cannot be restored.</p>\n<p><strong>Path parameters</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>id</td><td>string</td><td>The key ID to revoke. Example: <code>&quot;ak_abc&quot;</code>.</td></tr>\n</tbody></table>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>curl -X DELETE storage.now/keys/ak_abc \\\n  -H &quot;Authorization: Bearer $TOKEN&quot;</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>await fetch(&quot;https://storage.now/keys/ak_abc&quot;, {\n  method: &quot;DELETE&quot;,\n  headers: { &quot;Authorization&quot;: `Bearer ${TOKEN}` },\n});</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{&quot;deleted&quot;: true}</code></pre>\n<h1 id=\"audit\">Audit</h1>\n<p>Complete audit trail of every operation performed through the API. Every bucket creation, object upload, key rotation, and authentication event is recorded with the actor, resource, client IP, and timestamp. Use the audit log for security monitoring, compliance reporting, or debugging unexpected behavior.</p>\n<h2 id=\"audit-read\"><span class=\"method method--get\">GET</span> /audit</h2>\n<p>Queries the audit log with optional filtering by action type and cursor-based pagination. Events are returned in reverse chronological order — most recent events first.</p>\n<p>Use the <code>before</code> parameter for pagination: take the <code>ts</code> value of the last event in the current page and pass it as <code>before</code> in the next request to fetch older events.</p>\n<p><strong>Query parameters</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Parameter</th><th>Type</th><th>Default</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>action</td><td>string</td><td>—</td><td>Filter events by action type. For example, <code>&quot;object.upload&quot;</code> returns only upload events, <code>&quot;bucket.create&quot;</code> returns only bucket creation events. Omit to return all event types.</td></tr>\n<tr><td>limit</td><td>integer</td><td><code>50</code></td><td>Maximum number of events to return per page. Must be between 1 and 200.</td></tr>\n<tr><td>before</td><td>integer</td><td>—</td><td>Return only events with a timestamp strictly less than this value (Unix milliseconds). Use for pagination by passing the <code>ts</code> of the last event from the previous page.</td></tr>\n</tbody></table>\n<p><strong>Response</strong></p>\n<table class=\"ref-table\">\n<thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>items</td><td>array</td><td>Array of audit event objects, ordered by timestamp descending.</td></tr>\n<tr><td>items[].action</td><td>string</td><td>The event action. See the list of event actions below.</td></tr>\n<tr><td>items[].resource</td><td>string</td><td>The affected resource path or identifier.</td></tr>\n<tr><td>items[].detail</td><td>object|null</td><td>Additional event-specific data, such as the new bucket configuration or the number of deleted objects.</td></tr>\n<tr><td>items[].ip</td><td>string</td><td>Client IP address that triggered the event.</td></tr>\n<tr><td>items[].ts</td><td>integer</td><td>Event timestamp in milliseconds.</td></tr>\n</tbody></table>\n<p><strong>Event actions:</strong> <code>bucket.create</code>, <code>bucket.update</code>, <code>bucket.delete</code>, <code>bucket.empty</code>, <code>object.upload</code>, <code>object.delete</code>, <code>object.move</code>, <code>object.copy</code>, <code>signed_url.create</code>, <code>signed_url.batch</code>, <code>signed_url.upload_create</code>, <code>auth.login</code>, <code>auth.logout</code>, <code>key.create</code>, <code>key.revoke</code>.</p>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code># Get the 10 most recent upload events\ncurl &quot;storage.now/audit?action=object.upload&amp;limit=10&quot; \\\n  -H &quot;Authorization: Bearer $TOKEN&quot;\n\n# Paginate to the next page\ncurl &quot;storage.now/audit?action=object.upload&amp;limit=10&amp;before=1710892800000&quot; \\\n  -H &quot;Authorization: Bearer $TOKEN&quot;</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>const res = await fetch(&quot;https://storage.now/audit?action=object.upload&amp;limit=10&quot;, {\n  headers: { &quot;Authorization&quot;: `Bearer ${TOKEN}` },\n});\nconst { items } = await res.json();</code></pre>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{\n  &quot;items&quot;: [\n    {&quot;action&quot;: &quot;object.upload&quot;, &quot;resource&quot;: &quot;documents/reports/q1.pdf&quot;, &quot;detail&quot;: {&quot;size&quot;: 524288}, &quot;ip&quot;: &quot;203.0.113.42&quot;, &quot;ts&quot;: 1710892800000},\n    {&quot;action&quot;: &quot;object.upload&quot;, &quot;resource&quot;: &quot;avatars/alice.png&quot;, &quot;detail&quot;: {&quot;size&quot;: 45678}, &quot;ip&quot;: &quot;203.0.113.42&quot;, &quot;ts&quot;: 1710892700000}\n  ]\n}</code></pre>\n<h1 id=\"endpoints\">All Endpoints</h1>\n<table class=\"ref-table\">\n<thead><tr><th>#</th><th>Method</th><th>Path</th><th>Group</th><th>Description</th></tr></thead>\n<tbody>\n<tr><td>1</td><td>POST</td><td>/bucket</td><td>Buckets</td><td>Create a new bucket</td></tr>\n<tr><td>2</td><td>GET</td><td>/bucket</td><td>Buckets</td><td>List all buckets</td></tr>\n<tr><td>3</td><td>GET</td><td>/bucket/:id</td><td>Buckets</td><td>Get bucket details and stats</td></tr>\n<tr><td>4</td><td>PATCH</td><td>/bucket/:id</td><td>Buckets</td><td>Update bucket configuration</td></tr>\n<tr><td>5</td><td>DELETE</td><td>/bucket/:id</td><td>Buckets</td><td>Delete an empty bucket</td></tr>\n<tr><td>6</td><td>POST</td><td>/bucket/:id/empty</td><td>Buckets</td><td>Remove all objects from a bucket</td></tr>\n<tr><td>7</td><td>POST</td><td>/object/:bucket/*path</td><td>Objects</td><td>Upload (error if exists)</td></tr>\n<tr><td>8</td><td>PUT</td><td>/object/:bucket/*path</td><td>Objects</td><td>Upload or replace</td></tr>\n<tr><td>9</td><td>GET</td><td>/object/:bucket/*path</td><td>Objects</td><td>Download (authenticated)</td></tr>\n<tr><td>10</td><td>GET</td><td>/object/public/:bucket/*path</td><td>Objects</td><td>Download from public bucket</td></tr>\n<tr><td>11</td><td>HEAD</td><td>/object/:bucket/*path</td><td>Objects</td><td>Get metadata as headers</td></tr>\n<tr><td>12</td><td>GET</td><td>/object/info/:bucket/*path</td><td>Objects</td><td>Get metadata as JSON</td></tr>\n<tr><td>13</td><td>POST</td><td>/object/list/:bucket</td><td>Objects</td><td>List and search objects</td></tr>\n<tr><td>14</td><td>DELETE</td><td>/object/:bucket</td><td>Objects</td><td>Batch delete objects</td></tr>\n<tr><td>15</td><td>POST</td><td>/object/move</td><td>Objects</td><td>Move or rename an object</td></tr>\n<tr><td>16</td><td>POST</td><td>/object/copy</td><td>Objects</td><td>Copy an object</td></tr>\n<tr><td>17</td><td>POST</td><td>/object/sign/:bucket</td><td>Signed URLs</td><td>Create signed download URL(s)</td></tr>\n<tr><td>18</td><td>POST</td><td>/object/upload/sign/:bucket/*path</td><td>Signed URLs</td><td>Create signed upload URL</td></tr>\n<tr><td>19</td><td>GET</td><td>/sign/:token</td><td>Signed URLs</td><td>Download via signed URL</td></tr>\n<tr><td>20</td><td>PUT</td><td>/upload/sign/:token</td><td>Signed URLs</td><td>Upload via signed URL</td></tr>\n<tr><td>21</td><td>POST</td><td>/auth/register</td><td>Auth</td><td>Register a new actor</td></tr>\n<tr><td>22</td><td>POST</td><td>/auth/challenge</td><td>Auth</td><td>Request Ed25519 challenge</td></tr>\n<tr><td>23</td><td>POST</td><td>/auth/verify</td><td>Auth</td><td>Verify signature, get token</td></tr>\n<tr><td>24</td><td>POST</td><td>/auth/magic-link</td><td>Auth</td><td>Send magic link email</td></tr>\n<tr><td>25</td><td>GET</td><td>/auth/magic/:token</td><td>Auth</td><td>Verify magic link</td></tr>\n<tr><td>26</td><td>POST</td><td>/auth/logout</td><td>Auth</td><td>End current session</td></tr>\n<tr><td>27</td><td>POST</td><td>/keys</td><td>Keys</td><td>Create a scoped API key</td></tr>\n<tr><td>28</td><td>GET</td><td>/keys</td><td>Keys</td><td>List API keys</td></tr>\n<tr><td>29</td><td>DELETE</td><td>/keys/:id</td><td>Keys</td><td>Revoke an API key</td></tr>\n<tr><td>30</td><td>GET</td><td>/audit</td><td>Audit</td><td>Query the audit log</td></tr>\n</tbody></table>\n<h1 id=\"errors\">Error Codes</h1>\n<p>All errors return a consistent JSON structure with a machine-readable <code>code</code> and a human-readable <code>message</code>. The HTTP status code always matches the error category.</p>\n<pre><button class=\"cb\" onclick=\"cp(this)\">Copy</button><code>{\n  &quot;error&quot;: {\n    &quot;code&quot;: &quot;not_found&quot;,\n    &quot;message&quot;: &quot;Object 'reports/q1.pdf' does not exist in bucket 'documents'.&quot;\n  }\n}</code></pre>\n<table class=\"ref-table\">\n<thead><tr><th>Code</th><th>HTTP</th><th>When it occurs</th></tr></thead>\n<tbody>\n<tr><td>invalid_request</td><td>400</td><td>The request body is malformed, a required field is missing, or a parameter value is out of range.</td></tr>\n<tr><td>unauthorized</td><td>401</td><td>No Bearer token was provided, the token has expired, or the token is invalid.</td></tr>\n<tr><td>forbidden</td><td>403</td><td>The token is valid but does not have sufficient scope or permission for the requested operation. Also returned for expired signed URLs.</td></tr>\n<tr><td>not_found</td><td>404</td><td>The requested bucket, object, or resource does not exist. For public endpoints, also returned when a bucket is not public.</td></tr>\n<tr><td>conflict</td><td>409</td><td>A resource with the same identifier already exists. For example, creating a bucket with a name that is already taken, or using <code>POST</code> to upload to a path that is already occupied.</td></tr>\n<tr><td>too_large</td><td>413</td><td>The uploaded file exceeds the bucket&#39;s <code>file_size_limit</code> or the global 100 MB per-request limit.</td></tr>\n<tr><td>unsupported_type</td><td>415</td><td>The uploaded file&#39;s MIME type is not in the bucket&#39;s <code>allowed_mime_types</code> list.</td></tr>\n<tr><td>rate_limited</td><td>429</td><td>Too many requests in the current window. Check the <code>Retry-After</code> response header for the number of seconds to wait.</td></tr>\n<tr><td>internal</td><td>500</td><td>An unexpected server error occurred. If this persists, please report it.</td></tr>\n</tbody></table>\n<h1 id=\"extensions\">Extensions</h1>\n<p>Extensions provide optional capabilities beyond the core storage API. They are not required for basic file storage and may not be available on all deployments.</p>\n<table class=\"ref-table\">\n<thead><tr><th>Extension</th><th>Endpoints</th><th>Purpose</th></tr></thead>\n<tbody>\n<tr><td>MCP</td><td>2</td><td>Model Context Protocol integration for AI agents like Claude and ChatGPT. Exposes storage operations as MCP tools.</td></tr>\n<tr><td>OAuth 2.0</td><td>7</td><td>Third-party application authorization using RFC 6749 with PKCE. Enables building apps that access storage on behalf of users.</td></tr>\n</tbody></table>\n";
export const docsSidebarHtml = "  <a href=\"#overview\">API Reference</a>\n  <a href=\"#quickstart\" class=\"sub\">Quick Start</a>\n  <a href=\"#authentication\" class=\"sub\">Authentication</a>\n  <a href=\"#rate-limits\" class=\"sub\">Rate Limits</a>\n  <a href=\"#buckets\">Buckets</a>\n  <a href=\"#bucket-create\" class=\"sub\"><span class=\"sm sm-post\">POST</span>/bucket</a>\n  <a href=\"#bucket-list\" class=\"sub\"><span class=\"sm sm-get\">GET</span>/bucket</a>\n  <a href=\"#bucket-get\" class=\"sub\"><span class=\"sm sm-get\">GET</span>/bucket/:id</a>\n  <a href=\"#bucket-update\" class=\"sub\"><span class=\"sm sm-patch\">PATCH</span>/bucket/:id</a>\n  <a href=\"#bucket-delete\" class=\"sub\"><span class=\"sm sm-delete\">DELETE</span>/bucket/:id</a>\n  <a href=\"#bucket-empty\" class=\"sub\"><span class=\"sm sm-post\">POST</span>/bucket/:id/empty</a>\n  <a href=\"#objects\">Objects</a>\n  <a href=\"#object-upsert\" class=\"sub\"><span class=\"sm sm-put\">PUT</span>/object/:bucket/*path</a>\n  <a href=\"#object-create\" class=\"sub\"><span class=\"sm sm-post\">POST</span>/object/:bucket/*path</a>\n  <a href=\"#object-download\" class=\"sub\"><span class=\"sm sm-get\">GET</span>/object/:bucket/*path</a>\n  <a href=\"#object-download-public\" class=\"sub\"><span class=\"sm sm-get\">GET</span>/object/public/:bucket/*path</a>\n  <a href=\"#object-head\" class=\"sub\"><span class=\"sm sm-head\">HEAD</span>/object/:bucket/*path</a>\n  <a href=\"#object-info\" class=\"sub\"><span class=\"sm sm-get\">GET</span>/object/info/:bucket/*path</a>\n  <a href=\"#object-list\" class=\"sub\"><span class=\"sm sm-post\">POST</span>/object/list/:bucket</a>\n  <a href=\"#object-delete\" class=\"sub\"><span class=\"sm sm-delete\">DELETE</span>/object/:bucket</a>\n  <a href=\"#object-move\" class=\"sub\"><span class=\"sm sm-post\">POST</span>/object/move</a>\n  <a href=\"#object-copy\" class=\"sub\"><span class=\"sm sm-post\">POST</span>/object/copy</a>\n  <a href=\"#signed-urls\">Signed URLs</a>\n  <a href=\"#sign-create\" class=\"sub\"><span class=\"sm sm-post\">POST</span>/object/sign/:bucket</a>\n  <a href=\"#sign-upload-create\" class=\"sub\"><span class=\"sm sm-post\">POST</span>/object/upload/sign/:bucket/*path</a>\n  <a href=\"#sign-download\" class=\"sub\"><span class=\"sm sm-get\">GET</span>/sign/:token</a>\n  <a href=\"#sign-upload\" class=\"sub\"><span class=\"sm sm-put\">PUT</span>/upload/sign/:token</a>\n  <a href=\"#auth\">Auth</a>\n  <a href=\"#auth-register\" class=\"sub\"><span class=\"sm sm-post\">POST</span>/auth/register</a>\n  <a href=\"#auth-challenge\" class=\"sub\"><span class=\"sm sm-post\">POST</span>/auth/challenge</a>\n  <a href=\"#auth-verify\" class=\"sub\"><span class=\"sm sm-post\">POST</span>/auth/verify</a>\n  <a href=\"#auth-magic-link\" class=\"sub\"><span class=\"sm sm-post\">POST</span>/auth/magic-link</a>\n  <a href=\"#auth-magic-verify\" class=\"sub\"><span class=\"sm sm-get\">GET</span>/auth/magic/:token</a>\n  <a href=\"#auth-logout\" class=\"sub\"><span class=\"sm sm-post\">POST</span>/auth/logout</a>\n  <a href=\"#keys\">Keys</a>\n  <a href=\"#key-create\" class=\"sub\"><span class=\"sm sm-post\">POST</span>/keys</a>\n  <a href=\"#key-list\" class=\"sub\"><span class=\"sm sm-get\">GET</span>/keys</a>\n  <a href=\"#key-revoke\" class=\"sub\"><span class=\"sm sm-delete\">DELETE</span>/keys/:id</a>\n  <a href=\"#audit\">Audit</a>\n  <a href=\"#audit-read\" class=\"sub\"><span class=\"sm sm-get\">GET</span>/audit</a>\n  <a href=\"#endpoints\">All Endpoints</a>\n  <a href=\"#errors\">Error Codes</a>\n  <a href=\"#extensions\">Extensions</a>";
