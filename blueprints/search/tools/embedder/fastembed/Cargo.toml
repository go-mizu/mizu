[package]
name = "embedder-fastembed"
version = "0.1.0"
edition = "2024"

[dependencies]
fastembed = "5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
clap = { version = "4", features = ["derive"] }
libc = "0.2"

[profile.release]
opt-level = 3
lto = "thin"
