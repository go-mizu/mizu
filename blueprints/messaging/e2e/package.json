{
  "name": "messaging-e2e",
  "version": "1.0.0",
  "description": "E2E tests for Messaging Blueprint",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "test:auth": "playwright test tests/auth.spec.ts",
    "test:chat": "playwright test tests/chat.spec.ts",
    "test:messaging": "playwright test tests/messaging.spec.ts",
    "test:realtime": "playwright test tests/realtime.spec.ts",
    "test:settings": "playwright test tests/settings.spec.ts",
    "test:qr": "playwright test tests/qr-friend.spec.ts",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
