{{define "mix.exs.tmpl"}}# Code generated by sdkelixir. DO NOT EDIT.

defmodule {{.ModuleName}}.MixProject do
  use Mix.Project

  @version {{elixirQuote .Version}}
  @source_url {{elixirQuote .Homepage}}

  def project do
    [
      app: :{{.AppName}},
      version: @version,
      elixir: "~> 1.14",
      start_permanent: Mix.env() == :prod,
      deps: deps(),
      package: package(),
      docs: docs(),
      name: {{elixirQuote .ModuleName}},
      description: {{elixirQuote .Description}},
      source_url: @source_url
    ]
  end

  def application do
    [
      extra_applications: [:logger]
    ]
  end

  defp deps do
    [
      {:req, "~> 0.4"},
      {:jason, "~> 1.4"},
      {:ex_doc, "~> 0.30", only: :dev, runtime: false},
      {:dialyxir, "~> 1.4", only: [:dev, :test], runtime: false},
      {:credo, "~> 1.7", only: [:dev, :test], runtime: false}
    ]
  end

  defp package do
    [
      licenses: ["MIT"],
      links: %{"GitHub" => @source_url}
    ]
  end

  defp docs do
    [
      main: {{elixirQuote .ModuleName}},
      extras: ["README.md"]
    ]
  end
end
{{end}}