````md
{{define "README.md.tmpl"}}
# {{.Service.Name}} Python SDK

This package is generated from a transport-neutral contract.

## Install (uv)

```bash
uv pip install -e .
````

## Usage

```python
from {{.Package}} import OpenAI

client = OpenAI(api_key="YOUR_API_KEY")

# Explore resources via autocomplete, e.g.
# client.{{if .Resources}}{{(index .Resources 0).PyName}}{{else}}resources{{end}}
```

## Async usage

```python
import asyncio
from {{.Package}} import AsyncOpenAI

async def main() -> None:
    client = AsyncOpenAI(api_key="YOUR_API_KEY")
    try:
        # await client.<resource>.<method>(...)
        pass
    finally:
        await client.close()

asyncio.run(main())
```

## Notes

* The generated SDK uses `httpx` for HTTP transport.
* Types are Pydantic v2 models.
* If the API supports SSE streaming, `Stream` and `AsyncStream` are provided.

{{end}}

```
::contentReference[oaicite:0]{index=0}
```
